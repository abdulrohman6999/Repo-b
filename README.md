# Repo B - Sample Web App with CI/CD
This repository contains a simple Flask application with a reusable GitHub Actions workflow for CI/CD.
The workflow builds a Docker image and pushes it to Docker Hub, then deploys the application to a Kubernetes cluster.
